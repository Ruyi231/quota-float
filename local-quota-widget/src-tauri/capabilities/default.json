{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal permissions for the local-only quota widget",
  "windows": ["widget"],
  "permissions": ["core:default"]
}

